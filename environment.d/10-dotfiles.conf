# Read by systemd-environment-d-generator at user manager startup.
# Ensures XDG_CONFIG_HOME points at the dotfiles repo so XDG-respecting
# tools (fish, etc.) find their config in this checkout.
XDG_CONFIG_HOME=$HOME/src/github.com/fredsmith/dotfiles
